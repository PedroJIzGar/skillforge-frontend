# SkillForge — Frontend (Angular 17 + PrimeNG + Firebase Auth)

UI de laboratorios de aprendizaje: listado y detalle de labs, autenticación con Firebase y base para feedback en tiempo real.

> 📍 **Roadmap:** [docs/ROADMAP.md](docs/ROADMAP.md)

## Stack
- Angular 17 (standalone)
- PrimeNG + PrimeFlex + PrimeIcons
- Firebase Auth (via `@angular/fire`)
- HTTP Interceptor (Bearer ID Token)

## Requisitos
- Node 20+
- Angular CLI (`npm i -g @angular/cli`)
- Backend en `http://localhost:8080/api` (ajústalo en `environment.ts`)
